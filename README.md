# ClassLoop

ClassLoop runs as a desktop application on `main`.

```bash
./run.sh
```

The launcher installs missing dependencies when needed, then opens the checked-in ClassLoop UI in a native Electron window.
