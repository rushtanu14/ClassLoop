{
  "name": "classloop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite preview --host 127.0.0.1",
    "start": "vite preview --host 127.0.0.1",
    "build": "node -e \"require('fs').accessSync('dist/index.html')\""
  },
  "devDependencies": {
    "vite": "^7.3.2"
  }
}
